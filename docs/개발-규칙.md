## 개발 규칙

### 빌드 후 항상 배포까지
- 코드 변경 완료 시 → git add → git commit → git push 순서로 항상 배포까지 완료할 것
- Vercel 배포 URL(scm-app-five.vercel.app)에서 바로 확인 가능해야 함

### 문서/메모 저장 위치
- 모든 문서, 메모, 로직 정리는 `docs/` 폴더에 md 파일로 저장
