# 업무일지 - 2026-04-08

## 1. 발주장부 로직 분석

### 확인필요 판정 로직 확인
- **특별관리 상품 + CN 물류 상태**: 봉제여부/기타1회성 상품이 CN 창고도착 또는 내륙운송중일 때 확인필요 표시
- **입고예정일 도래**: 답변에 기록된 arrivalDate가 오늘 이하일 때 확인필요 표시

### DB 저장 구조 확인
- `orderbook_notes`: 답변/입고예정일 메모 (덮어쓰기 방식, 스프레드시트에서 사라진 건은 자동 정리)

## 2. 품절 기록 DB 로직 분석

| 데이터 | localStorage | DB (Vultr) | 쌓임 방식 | 정리 로직 |
|---|---|---|---|---|
| 현재 사유 (`soldout_reasons_v2`) | O | O | 덮어쓰기 | 수동삭제 + 재진입 자동삭제 |
| 누적 기록 (`soldout_history`) | O | O | 무한 누적 | 없음 (이력 보존) |
| 신규 상품 재고 추적 (`new_product_stock`) | O | O | 바코드별 기록 | 7일 초과 자동 삭제 |
| 이전 품절 목록 (`soldout_prev_barcodes`) | O | X | 덮어쓰기 | 매번 교체 |

## 3. 레거시 함수명 정리 (sheetSync.js)

스프레드시트 시절 함수명을 DB 기반으로 리네이밍:

| 변경 전 | 변경 후 |
|---|---|
| `fetchFromSheet()` | `fetchReasons()` |
| `saveReasonsToSheet()` | `saveReasons()` |
| `deleteReasonFromSheet()` | `deleteReason()` |
| `markLocalSave()` | 제거 |

**적용 파일**: `sheetSync.js`, `SoldOut.jsx`, `SoldOutHistory.jsx`
