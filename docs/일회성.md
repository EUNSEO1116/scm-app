# 일회성 작업 메모 (2026-04-07)

## Vercel 프록시
- `api/proxy.js` 생성 — HTTPS(Vercel) → HTTP(Vultr) 프록시 서버리스 함수
- `vercel.json`에 rewrite 규칙 추가: `/api/proxy/:path*` → `/api/proxy`

## 추가 수정 파일
- `src/pages/OrderRequest.jsx` — Apps Script URL → Google Sheets CSV 직접 읽기로 변경

## 삭제 가능한 스프레드시트 시트
- ~~달력데이터~~ → DB로 이관 완료, 삭제 가능
- ~~주의 품목~~ → DB로 이관 완료, 삭제 가능
- ~~품절 기록~~ → DB로 이관 완료, 삭제 가능
- **특별 관리 상품** → 읽기용으로 계속 사용, 삭제 금지

## 데이터 마이그레이션 완료
- 품절 사유: 28건
- 캘린더 이벤트: 9건
- 제외 항목: 16건
